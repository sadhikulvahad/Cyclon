<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cyclon Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      padding: 0 15px; /* To ensure padding on mobile devices */
    }

    .logo {
      position: absolute;
      top: 20px;
      left: 20px;
      max-width: 100px; /* Limit logo size */
      height: auto;
    }

    .card {
      width: 100%;
      max-width: 400px; /* Limit card width for larger screens */
      padding: 2rem;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    }

    .submit-btn {
      background-color: #2c3e50;
      border-color: #2c3e50;
    }

    .submit-btn:hover {
      background-color: #34495e;
      border-color: #34495e;
    }

    @media (max-width: 768px) {
      /* Adjustments for smaller screens (phones) */
      .logo {
        display: none; /* Hide logo on small screens */
      }

      .card {
        padding: 1.5rem; /* Reduce padding on smaller screens */
      }
    }

    @media (min-width: 1200px) {
      /* Adjustments for larger screens (e.g., TVs) */
      .card {
        max-width: 500px; /* Increase card size on larger screens */
        padding: 3rem;
      }
    }

    @media (min-width: 576px) and (max-width: 1199.98px) {
      /* Adjustments for tablets and medium screens */
      .card {
        max-width: 80%; /* Adjust card size for tablets */
      }
    }
  </style>
</head>

<body>
  <img src="/images/Logo.png" alt="Logo" class="logo">
  <div class="card">
    <h2 class="text-center mb-4">Enter Your Mail Id</h2>
    <p class="text-center text-muted mb-4">You will receive OTP in your mail id</p>
    {{#if error}}
    <div>{{error}}</div>
    {{/if}}
    <form id="forgotPasswordForm" action="/forgotPass" method="POST">
      <div class="mb-3">
        <input type="email" class="form-control" id="email" name="email" placeholder="Email" required>
      </div>
      <button type="submit" class="btn btn-primary w-100 submit-btn">Submit</button>
    </form>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
